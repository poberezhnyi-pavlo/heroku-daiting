version: '3'
services:
  php:
    build:
      context: config/php
      args:
        PUID: $PUID
        PGID: $PGID
    volumes:
      - ../:/var/www
    environment:
      XDEBUG_CONFIG: "remote_host=192.168.220.1 remote_enable=1"
#      XDEBUG_CONFIG: "remote_host=host.docker.internal remote_enable=1" #for Windows/Mac
      PHP_IDE_CONFIG: "serverName=Docker"
    depends_on:
      - mysql
    links:
      - mysql:db
    networks:
      - internal
  nginx:
    build:
      context: config/nginx
      args:
        PUID: $PUID
        PGID: $PGID
    volumes:
      - ../:/var/www
    ports:
      - 80:80
    depends_on:
      - php
    networks:
      - internal
  mysql:
    image: mysql:8
    command: --default-authentication-plugin=mysql_native_password
    restart: always
    environment:
      MYSQL_ROOT_PASSWORD: example
      MYSQL_DATABASE: dating
      MYSQL_USER: root
      MYSQL_PASSWORD: example
    volumes:
      - ./data/mysql:/var/lib/mysql
    ports:
      - 3306:3306
    networks:
      - internal
  adminer:
    image: adminer
    restart: always
    ports:
      - 8080:8080
    networks:
      - internal
networks:
  internal:
    driver: bridge
    ipam:
      driver: default
      config:
        - subnet: 192.168.220.0/28
